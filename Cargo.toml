[package]
name = "bevy_tween"
description = "Flexible tweening plugin library for Bevy"
version = "0.2.0"
edition = "2021"
authors = [ "Multirious" ]
license = "MIT OR Apache-2.0" 
repository = "https://github.com/Multirious/bevy_tween"
homepage = "https://github.com/Multirious/bevy_tween"
documentation = "https://docs.rs/bevy_tween"
readme = "README.md"
keywords = [ "bevy", "tween", "tweening", "easing", "animation" ]
categories = [ "game-development" ]

[workspace]
resolver = "2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = { version = "0.13.0", default-features = false }

[dev-dependencies]
bevy-inspector-egui = "0.23"
bevy = { version = "0.13.0", default-features = false, features = [ "bevy_winit", "bevy_asset", "bevy_render", "bevy_sprite", "tonemapping_luts", "png" ] }
rand = "0.8.5"

[features]
default = [
  "span_tween",
  "bevy_asset",
  "bevy_render",
  "bevy_sprite"
]

# Tween player implementation by defining a tween in range of time.
span_tween = []

# Enable bevy/bevy_asset feature.
# Adds tweening system for asset
bevy_asset = [ "bevy/bevy_asset" ]
# Enable bevy/bevy_render feature.
# Adds nothing just yet but required by the "bevy_sprite" feature.
bevy_render = [ "bevy/bevy_render"]
# Enable bevy/bevy_sprite feature.
# Add some built-in interpoaltor related to sprite
bevy_sprite = [ "bevy/bevy_sprite", "bevy_render" ]

[package.metadata.docs.rs]
features = [
  "span_tween",
  "bevy_asset",
  "bevy_render",
  "bevy_sprite"
]

[[example]]
name = "animated_banner"
required-features = [
  "bevy_sprite",
  "bevy_asset",

  "span_tween",
]

[[example]]
name = "demo_follow"
required-features = [
  "bevy_sprite",
  "bevy_asset",

  "span_tween",
]

[[example]]
name = "demo_click"
required-features = [
  "bevy/bevy_winit",
  "bevy/png",

  "bevy_sprite",
  "bevy_asset",

  "span_tween",
]
