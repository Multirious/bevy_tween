[package]
name = "span_tween_editor"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy_tween = { path = ".."}
bevy = { version = "0.13", default-features = false, features = [ "bevy_ui" ] }
